package we

import (
	"github.com/svenliebig/work-environment/pkg/context"
)

func Use(ctx context.BaseContext) error {
	// projects := ctx.Configuration().Projects

	// for _, project := range projects {
	// 	if err := git.Checkout(project.Path, project.Git.RemoteUrl); err != nil {
	// 		return err
	// 	}
	// }

	return nil
}
